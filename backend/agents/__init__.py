"""Specialist UFC agent system. Replaces the single generalist run_analysis()
with 20 parallel domain-specific scouting reports + a synthesis layer that
produces betting recommendations. See ../UFC agents/README.md for the framework."""
